<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item>
        <shape>
            <corners
                android:bottomLeftRadius="@dimen/rounded_button"
                android:bottomRightRadius="@dimen/rounded_button"
                android:topLeftRadius="@dimen/rounded_button"
                android:topRightRadius="@dimen/rounded_button" />
            <gradient
                android:angle="90"
                android:startColor="@color/primary_color"
                android:centerColor="@color/primary_color"
                android:endColor="@color/primary_color"
                 />
        </shape>
    </item>

</layer-list>
